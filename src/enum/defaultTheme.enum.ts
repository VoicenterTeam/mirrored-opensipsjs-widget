import type { IWidgetTheme } from '@/types/public-api'

export const defaultTheme: IWidgetTheme = {
    colors: {
        primary: '#00a0df',
    },
    layoutConfig: {
        type: 'rounded',
        mode: 'floating'
    }
}
